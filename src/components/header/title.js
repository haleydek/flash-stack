import React from 'react';
import styled from 'styled-components/macro';

const Title = () => {
    return <StyledTitle>Flash Stack</StyledTitle>
}

export default Title;

const StyledTitle = styled.div`
    font-size: 3em;
`;